recv_packet
0
# Ejecutar dentro de una condición tipo recv_packet
if packet.startswith("dlgi"):
    # El paquete contiene múltiples opciones separadas por espacios, cada una inicia con "#u_i"
    opciones = [segmento for segmento in packet.split(" ") if segmento.startswith("#u_i")]
    if len(opciones) >= 2:
        # Tomamos la segunda opción y sustituimos el id del personaje por el actual
        datos_opcion = opciones[1].split("^")
        if len(datos_opcion) >= 3:
            datos_opcion[2] = str(self.id)
            datos = "^".join(datos_opcion)
            time.sleep(self.randomize_delay(1, 1.5))
            print(f"25-[MEMBER] {self.name} confirm wing of return")
            self.api.send_packet(datos)
	




