periodical
0
if int(self.map_id) == 22 and int(self.attr1) == 48:
	print(f"[MEMBER] {self.name} Use amulet of return to talk with Frigg")
	cond.off = 0
	time.sleep(self.randomize_delay(2,3))
	cond.on = 72
	cond.on = 73
	self.use_item(2071, "etc") #use amulet of return
	time.sleep(self.randomize_delay(10,12)) #wait to accept amulet of return in next condition

elif int(self.map_id) == 1 and int(self.attr1) == 48:
	print(f"[MEMBER] {self.name} Use amulet of return from Nosville to talk with Frigg")
	cond.off = 0
	time.sleep(self.randomize_delay(2,3))
	cond.on = 72
	cond.on = 73
	self.use_item(2071, "etc") #use amulet of return
	time.sleep(self.randomize_delay(12,14)) #wait to accept amulet of return in next condition
