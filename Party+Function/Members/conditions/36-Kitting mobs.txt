recv_packet
0
import random
if packet.startswith(f"su 1 {self.id} 3") and time.time() - self.attr15 > random.randint(2,4):
	self.attr15 = time.time()
	self.api.player_walk(self.pos_x + random.choice([-2,2]), self.pos_y + random.choice([-2,2]))