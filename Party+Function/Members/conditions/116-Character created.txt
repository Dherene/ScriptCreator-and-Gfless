recv_packet
0
if packet.startswith(f"clist 0 {self.name}") and int(self.attr1) == 73:
        time.sleep(self.randomize_delay(3,4))
        print(f"{self.nameacc} has been created character")
        self.api.send_packet("select 0")
        time.sleep(self.randomize_delay(1,2))
        cond.on = 1
        self.attr90 = 0
        self.attr1 = 0