periodical
0
if int(self.map_id) == 1 and int(self.attr1) == 12:
	print(f"[MEMBER] {self.name} walk to Nosville Tower")
	cond.off = 0
	time.sleep(self.randomize_delay(1,2))
	# Marca primer personaje que llega a Nosville como dueño del punto de retorno
	if getattr(selfgroup, "return_point_owner", None) is None:
		selfgroup.return_point_owner = str(self.id)
		selfgroup.return_point_owner_name = self.name
		print(f"[RETURN] {self.name} marked as return point owner")
	else:
		print(f"[RETURN] {self.name} arrived but owner already set: {getattr(selfgroup, 'return_point_owner_name', 'unknown')}")
	cond.on = 26
	cond.on = 27
	#self.walk_to_point([64,107], 3)
	time.sleep(self.randomize_delay(3,4))
	self.walk_to_point([68,57], 1)
	self.attr1 = 13

