recv_packet
0
import random
import string
import time
if packet.startswith("infoi 875 0 0 0"):
	time.sleep(self.randomize_delay(3,4))
	print(f"{self.nameacc} retry create other character")
	self.name = self.rolename()
	gender = random.randint(0,1)
	hair_style = random.randint(0,1)
	hair_type = random.randint(0,9)
	self.api.send_packet(f"Char_NEW_JOB {self.name} 0 {gender} {hair_style} {hair_type} 4")
	print(f"[MEMBER] {self.nameacc} created character {self.name}")

	
	