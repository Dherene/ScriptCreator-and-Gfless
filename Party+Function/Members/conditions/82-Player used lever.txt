recv_packet
0
if packet.startswith("tp 1") and int(self.attr1) == 53:
	print(f"[MEMBER] {self.name} Used first lever")
	cond.off = 0
	time.sleep(self.randomize_delay(0.75,1))
	cond.on = 83
	time.sleep(self.randomize_delay(4,5))
	self.attr1 = 54