recv_packet
0
if int(self.attr3) == 6 and packet.startswith("gidx 1") and int(self.attr6) == 1:
	self.api.start_bot()
	cond.off = 41
	cond.on = 43
	self.attr3 = 7
	print(f"40-[MEMBER][TS56] {self.name} start room 3")