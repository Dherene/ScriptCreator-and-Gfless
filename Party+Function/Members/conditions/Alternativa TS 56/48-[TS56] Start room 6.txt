recv_packet
0
if int(self.attr3) == 12 and packet.startswith("gidx 1") and int(self.attr6) == 1:
	self.api.start_bot()
	cond.off = 47
	cond.on = 49
	self.attr3 = 13
	print(f"46-[MEMBER][TS56] {self.name} start room 6")