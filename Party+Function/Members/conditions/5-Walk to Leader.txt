periodical
1
import time
1
if int(self.map_id) == 1 and int(self.attr1) == 3 and int(self.attr90) == 0:
        print(f"1-[MEMBER] {self.name} walk to leader")
        cond.off = 0
        time.sleep(self.randomize_delay(1,2))
        x = int(selfgroup.leader_x)
        y = int(selfgroup.leader_y)
        self.walk_to_point([x, y], 3)
        cond.on = 6
        self.attr90 = 1 #desactivar esta condición y activar la siguiente
        self.attr95 = 1  # modo de comercio: recibir objetos
        self.attr6 = 99   #random valor if attr6 = 1 activate ts 56
        self.attr3 = 99    #random valor if attr3  = 1 reset rooms ts 56