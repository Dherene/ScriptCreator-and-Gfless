recv_packet
0
if int(self.attr3) == 8 and packet.startswith("gidx 1") and int(self.attr6) == 1:
	cond.off = 59
	cond.on = 61
	self.api.start_bot()
	self.attr3 = 9
	print(f"42-[MEMBER][TS56] {self.name} start room 4")