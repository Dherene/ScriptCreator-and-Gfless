send_packet
0
if packet.startswith("#u_i") and int(self.map_id) == 22 and int(self.attr1) == 9:
    segments = packet.split("^")
    if len(segments) >= 6 and segments[5] == "2":
        cond.on = 37
        time.sleep(self.randomize_delay(1,2))
        self.attr1 = 10
