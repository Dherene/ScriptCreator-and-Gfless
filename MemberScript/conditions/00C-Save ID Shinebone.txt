recv_packet
1
splitPacket = packet.split()   #reutilizar esta plantilla cambiarla para que se adapte a Shinebone
if (
    len(splitPacket) >= 6
    and splitPacket[0] == "in"
    and splitPacket[1] == "2"
    and splitPacket[2] == "906"
):
    vnum = int(splitPacket[3])
    x = int(splitPacket[4])
    y = int(splitPacket[5])
    if not hasattr(self, "fruit_trees"):
        self.fruit_trees = []                                #reemplace name fruit_trees for Shinebone
    self.fruit_trees.append({"vnum": vnum, "x": x, "y": y}) #reemplace name fruit_trees for Shinebone
    print(f"Saved Fruit Tree: vnum={vnum}, x={x}, y={y}")  #reemplace name fruit_trees for Shinebone