recv_packet
1
if packet.startswith(f"sayi2 1 {self.id} 10 177 1 {self.leadername}"):
	print("Leader rejected invitation")
	time.sleep(self.randomize_delay(10,15))
	self.api.send_packet(f"req_exc 1 {self.leaderID}")