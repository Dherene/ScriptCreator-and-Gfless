recv_packet
0
if int(self.attr3) == 10 and packet.startswith("gidx 1") and int(self.attr6) == 1:
	cond.off = 61
	cond.on = 63
	self.api.start_bot()
	self.attr3 = 11
	print(f"44-[MEMBER][TS56] {self.name} start room 5")