periodical
0
if int(self.attr1) == 0 and int(self.attr90) == 1 and selfgroup.trade == "0":
        selfgroup.trade = "1" #dissable option the memmber enter in trade with leader
        cond.off = 15
        self.attr90 = 2  # desactivar esta condición y activar la siguiente
        time.sleep(self.randomize_delay(0.75,1.5))
        self.api.send_packet(f"req_exc 1 {self.leaderID}")
        cond.on = 18
        cond.on = 17
        print(f"2-[MEMBER] {self.name} sending trade request")