periodical
1
if int(self.map_id) == 1 and int(self.pos_x) == 68 and int(self.pos_y) == 57 and int(self.attr5) == 2:
	print(f"28-[MEMBER] {self.name} use tower nosville")
	ID = self.TowerTP["ID"]
	self.api.send_packet(f"ncif 2 {ID}")
	time.sleep(self.randomize_delay(1,2))
	self.api.send_packet(f"n_run 16 1 2 {ID}")
	time.sleep(self.randomize_delay(2,3))
	